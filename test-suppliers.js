const admin = require("firebase-admin");
const serviceAccount = require("./firebase-admin-key.json"); // Assuming this exists or using default env vars. Wait, the app uses FIREBASE_SERVICE_ACCOUNT_KEY.
